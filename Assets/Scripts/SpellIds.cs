// Same shape as ItemIds - one entry per spell known to exist. Only one today (Orbe de Foudre,
// see PlayerController's Lightning Orb fields) but this is where a future spell gets its id.
public static class SpellIds
{
    public const string LightningOrb = "lightning_orb";
}
