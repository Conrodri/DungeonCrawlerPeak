// One floor = one biome, rolled once per floor in DungeonGenerator.Build (see BiomeTheme for the
// visual palette each one maps to).
public enum Biome { Jungle, Forest, City, Beach, Cave, SkyCastle }
